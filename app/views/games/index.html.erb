<%= link_to 'New template', new_game_path %><br/>


<table>
    <tr>
      <th>Afbeelding team 1</th>
      <th>Score 1</th>
      <th>Score 2</th>
      <th>Afbeelding team 2</th>
    </tr>

    <% @games.each do |game| %>

      <tr> 
        <td><%= game.gameteams.name%></td>
        <td><%= game.score1 %></td>
        <td><%= game.score2 %></td>
        

        <td><%= button_to 'Destroy', game, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
  <% end %>

</table>
<br/>
<br/>
<br/>


<table>
    <tr>
      <th>Afbeelding team 1</th>
      <th>Score 1</th>
      <th>Score 2</th>
      <th>Afbeelding team 2</th>
    </tr>

    <% Gameteam.all.each do |f| %>

      <tr> 
        <td><%= image_tag Team.find(f.team1_id).avatar.url(:thumb) %></td>
        <td><%= Game.find(f.game_id).score1 %></td>
        <td><%= Game.find(f.game_id).score2 %></td>
        <td><%= image_tag Team.find(f.team2_id).avatar.url(:thumb) %></td>
      </tr>
  <% end %>

</table>




<% current_user.poolmemberships.each do |pool| %>
      <li>
        <%= link_to pool_path(pool.pool_id),:class => 'grey' do %>
          <%= Pool.find(pool.pool_id).name %>
        <% end %>
      </li>
    <% end %>
<br/>
<br/>
<br/>

<table>
    <tr>
      <th>Afbeelding</th>
      <th>Naam</th>
      <th>Poule</th>
      <th>opbokken</th>
    </tr>

    <% @teams.each do |team| %>
      <tr class="eventlist1"> 
        <td>
          <%= image_tag team.avatar.url(:thumb) %>    
        </td>
        <td><%= team.name %></td>
        <td><%= team.poule %></td>

        <td><%= button_to 'Destroy', team, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
  <% end %>
</table>